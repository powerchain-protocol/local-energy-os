import{NextResponse}from"next/server";import{tokenStore}from"@/lib/proof-of-energy/store";export async function POST(req:Request){const{tokenId,reason}=await req.json()as{tokenId:string;reason?:string};const token=tokenStore.get(tokenId);if(!token)return NextResponse.json({error:"Token not found"},{status:404});if(token.status==="retired"||token.status==="burned")return NextResponse.json({error:"Token is already final"},{status:409});const data={...token,status:"retired"as const,retiredAt:new Date().toISOString(),reason:reason??"Energy consumed"};tokenStore.set(tokenId,data);return NextResponse.json({data})}
