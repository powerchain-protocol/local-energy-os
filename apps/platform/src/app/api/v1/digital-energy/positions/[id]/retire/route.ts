import type { EnergyRetirementReason } from "@powerchain/energy-core";
import { digitalEnergyError, digitalEnergyResponse, enforceDigitalEnergyRateLimit, getDigitalEnergyContext, requireIdempotencyKey, retireDigitalEnergyPosition } from "@/lib/digital-energy/server";
export async function POST(request: Request, { params }: { params: Promise<{ id: string }> }) { const context=await getDigitalEnergyContext(request); try { enforceDigitalEnergyRateLimit(request,context,true); const {id}=await params; const body=await request.json() as {retirementId:string;reason:EnergyRetirementReason}; const data=await retireDigitalEnergyPosition(context,{positionId:id,retirementId:String(body.retirementId),reason:body.reason,idempotencyKey:requireIdempotencyKey(request)}); return digitalEnergyResponse(data,context); } catch(error){ return digitalEnergyError(error,context); } }
