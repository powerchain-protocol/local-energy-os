"use client";
import {ArrowUpRight,CalendarDays,MapPin,Users} from "lucide-react";
import {ButtonLink} from "@/components/ui/button";
import {Badge} from "@/components/ui/badge";
export type FundingProject={id:string;slug:string;title:string;summary:string;raised:number;goal:number;backers:number;daysLeft:number;location?:string;assetType?:string;capacity?:string};
export function ProjectCard({project}:{project:FundingProject}){const pct=Math.min(100,Math.round(project.raised/project.goal*100));return <article className="group overflow-hidden rounded-[22px] border border-[var(--border)] bg-[var(--surface)] shadow-[0_12px_34px_rgba(15,23,42,.05)] transition-all duration-200 hover:-translate-y-1 hover:shadow-[0_22px_50px_rgba(15,23,42,.09)]"><div className="image-placeholder min-h-[190px]"><div className="absolute inset-x-0 top-0 flex items-center justify-between p-4"><div className="flex items-center gap-2"><Badge variant="dark-green">{project.assetType??"Renewable project"}</Badge><span className="project-mvp rounded-full px-2.5 py-1 text-[10px] font-semibold">MVP</span></div><span className="rounded-full bg-white/90 px-2.5 py-1 text-[11px] font-semibold text-slate-800 shadow-sm">{project.id}</span></div><div className="absolute inset-x-4 bottom-4 rounded-[16px] border border-white/30 bg-white/80 p-3 backdrop-blur-md dark:bg-slate-950/70"><div className="flex items-center justify-between text-xs"><span className="inline-flex items-center gap-1.5 font-semibold"><MapPin className="h-3.5 w-3.5"/>{project.location??"PowerChain Network"}</span><span className="font-semibold text-emerald-800 dark:text-emerald-300">{project.capacity??"Verified capacity"}</span></div></div></div><div className="p-5"><h2 className="text-xl font-semibold tracking-[-.025em]">{project.title}</h2><p className="mt-2 line-clamp-2 text-sm leading-6 text-[var(--muted)]">{project.summary}</p><div className="mt-5 flex items-end justify-between gap-4"><div><p className="text-[11px] font-semibold uppercase tracking-[.12em] text-[var(--muted)]">Raised</p><strong className="mt-1 block text-xl font-semibold">€{project.raised.toLocaleString()}</strong></div><span className="text-sm font-semibold text-emerald-700">{pct}% funded</span></div><div className="mt-3 h-2 overflow-hidden rounded-full bg-black/[.07] dark:bg-white/10"><div className="h-full rounded-full bg-gradient-to-r from-emerald-700 to-emerald-500" style={{width:`${pct}%`}}/></div><div className="mt-4 grid grid-cols-3 gap-2 rounded-[16px] bg-black/[.025] p-3 text-xs dark:bg-white/[.035]"><span><Users className="mb-1 h-3.5 w-3.5 text-emerald-700"/><strong className="block font-semibold">{project.backers}</strong><small className="text-[var(--muted)]">Backers</small></span><span><CalendarDays className="mb-1 h-3.5 w-3.5 text-emerald-700"/><strong className="block font-semibold">{project.daysLeft}</strong><small className="text-[var(--muted)]">Days left</small></span><span><strong className="block font-semibold">€{project.goal.toLocaleString()}</strong><small className="text-[var(--muted)]">Funding goal</small></span></div><div className="mt-4 rounded-2xl border border-amber-500/20 bg-amber-500/[.06] p-3 text-xs text-amber-900 dark:text-amber-200">MVP preview: financing information is illustrative until project verification is completed.</div><ButtonLink href={`/explore/projects/${project.slug}`} variant="framed" className="mt-4 w-full justify-between">Explore project <ArrowUpRight className="h-4 w-4"/></ButtonLink></div></article>}
