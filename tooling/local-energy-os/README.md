# Local Energy OS Tooling

Configuration-owned protocol capabilities and runtime safety/degraded-service policy declarations. Runtime code in `@powerchain/system-management` remains the executable authority; these files support operational tooling, validation, and documentation generation.
