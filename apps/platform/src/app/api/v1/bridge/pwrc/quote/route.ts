import{NextRequest,NextResponse}from"next/server";import{createPwrcBridgeQuote}from"@/lib/blockchain/bridge";import type{PwrcBridgeDirection}from"@/types/bridge/pwrc";
export async function GET(req:NextRequest){try{const direction=req.nextUrl.searchParams.get("direction") as PwrcBridgeDirection;const amount=req.nextUrl.searchParams.get("amount")??"";if(!["powerchain-to-solana","solana-to-powerchain"].includes(direction))return NextResponse.json({error:"Invalid bridge direction"},{status:400});return NextResponse.json({data:createPwrcBridgeQuote(direction,amount)})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Invalid quote"},{status:400})}}
