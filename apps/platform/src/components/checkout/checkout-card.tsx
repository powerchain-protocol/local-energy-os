"use client";
import {useState} from "react";
import {Button} from "@/components/ui/button";
export function CheckoutCard(){const[asset,setAsset]=useState("USDC");const[amount,setAmount]=useState("25");const[state,setState]=useState("Ready for wallet authorization");async function submit(){setState("Creating secure checkout…");const response=await fetch("/api/v1/checkout",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({wallet:"11111111111111111111111111111111",asset,amount:Number(amount)})});const body=await response.json();setState(response.ok?`Checkout ${body.data?.status??"created"}`:body.error??"Checkout failed")}return <div className="rounded-[24px] border border-[var(--border)] bg-[var(--surface)] p-6 shadow-sm"><h2 className="text-xl font-semibold">Renewable checkout</h2><p className="mt-2 text-sm text-[var(--muted)]">Pay with supported wallet assets. Final execution requires an explicit wallet signature.</p><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-sm">Asset<select value={asset} onChange={(e)=>setAsset(e.target.value)} className="mt-1 w-full rounded-xl border bg-transparent p-3"><option>USDC</option><option>PWRC</option><option>SOL</option></select></label><label className="text-sm">Amount<input value={amount} onChange={(e)=>setAmount(e.target.value)} className="mt-1 w-full rounded-xl border bg-transparent p-3" inputMode="decimal"/></label></div><Button onClick={()=>void submit()} className="mt-5 w-full">Continue to authorization</Button><p className="mt-3 text-xs text-[var(--muted)]">{state}</p></div>}
