import{NextRequest,NextResponse}from"next/server";import{getChat}from"@/lib/ai/chat-store";export async function GET(req:NextRequest,{params}:{params:Promise<{id:string}>}){const{id}=await params;const userId=req.nextUrl.searchParams.get("userId");if(!userId)return NextResponse.json({error:"userId is required"},{status:400});const chat=getChat(id,userId);return chat?NextResponse.json({data:chat}):NextResponse.json({error:"Chat not found"},{status:404})}
