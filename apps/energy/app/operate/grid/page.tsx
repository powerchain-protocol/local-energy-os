"use client";
import { DataTable, DataValue, EmptyState, InlineNotice, PageHeader, Panel, Skeleton, StatusBadge, TableToolbar } from "@powerchain/ui";
import { useEnergyResource } from "../../../components/use-energy-resource";
type Feeder={id:string;code:string;name?:string}; type Transformer={id:string;code:string;name?:string;feeders?:Feeder[]}; type Substation={id:string;code:string;name?:string;transformers?:Transformer[]}; type Area={id:string;code:string;name:string;country?:string;substations?:Substation[]};
export default function GridPage(){const areas=useEnergyResource<{items:Area[]}>("/api/v1/grid/areas");return <main className="pc-page ems-page"><PageHeader eyebrow="EMS · Grid" title="Grid" description="Connection areas, topology and operational constraints. Live import/export and outages require telemetry or grid-operator integration." action={<StatusBadge tone={areas.error?"warning":"info"}>TOPOLOGY</StatusBadge>}/><InlineNotice title="Topology is not live grid state" tone="info" icon="grid">Grid areas can be configured before live limits, outages, feeder loading or connection-point exchange telemetry are available.</InlineNotice><div className="pc-grid ems-grid-gap"><Panel className="pc-span-12" eyebrow="Grid twin" title="Configured grid areas" description="Substations, transformers and feeders establish the structural context for later constraints and routing.">{areas.loading?<Skeleton lines={5}/>:<><TableToolbar title="Grid areas" count={areas.data?.items.length??0}/><DataTable rows={areas.data?.items??[]} rowKey={r=>r.id} columns={[
{key:"area",header:"Area",cell:r=><DataValue value={r.name} meta={r.code}/>},{key:"substations",header:"Substations",align:"right",cell:r=><DataValue value={r.substations?.length??0}/>},{key:"transformers",header:"Transformers",align:"right",cell:r=><DataValue value={(r.substations??[]).reduce((n,s)=>n+(s.transformers?.length??0),0)}/>},{key:"feeders",header:"Feeders",align:"right",cell:r=><DataValue value={(r.substations??[]).reduce((n,s)=>n+(s.transformers??[]).reduce((m,t)=>m+(t.feeders?.length??0),0),0)}/>}
]} empty={<EmptyState icon="grid" title="No grid topology configured" description="Add grid areas, substations, transformers and feeders before enabling grid-aware routing or constraint views."/>}/></>}</Panel></div></main>}
