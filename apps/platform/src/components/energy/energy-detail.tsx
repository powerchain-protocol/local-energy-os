import Link from "next/link";
import { ROUTES } from "@/config/routes";

export function EnergyDetail({ title, subtitle, metrics }: { title: string; subtitle: string; metrics: Array<{ label: string; value: string; detail: string }> }) {
  return <div className="space-y-6"><div><Link href={ROUTES.energy} className="text-sm font-semibold text-emerald-700 dark:text-emerald-400">← Energy command center</Link><h1 className="page-title">{title}</h1><p className="page-subtitle">{subtitle}</p></div><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(metric=><article key={metric.label} className="panel p-5"><p className="text-sm font-semibold text-[var(--muted)]">{metric.label}</p><p className="mt-3 text-3xl font-black">{metric.value}</p><p className="mt-2 text-xs text-[var(--muted)]">{metric.detail}</p></article>)}</section><section className="panel min-h-[22rem] p-6"><p className="eyebrow">Live operations</p><h2 className="mt-2 text-xl font-extrabold">Operational timeline</h2><div className="mt-6 grid gap-3">{["Telemetry synchronized", "Forecast model updated", "Dispatch plan validated", "Market availability published"].map((item,index)=><div key={item} className="flex items-center gap-3 rounded-xl border border-[var(--border)] p-3"><span className="grid h-7 w-7 place-items-center rounded-full bg-emerald-500/10 text-xs font-bold text-emerald-700">{index+1}</span><span className="text-sm font-semibold">{item}</span><span className="ml-auto text-xs text-[var(--muted)]">Live</span></div>)}</div></section></div>;
}
