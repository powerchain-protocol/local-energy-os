import { CheckCircledIcon, ExclamationTriangleIcon, GlobeIcon, LightningBoltIcon } from "@radix-ui/react-icons";
import type { IntegrationHealth } from "@powerchain/integration/core";
const stateClass: Record<IntegrationHealth["state"], string> = { available: "bg-emerald-100 text-emerald-800", degraded: "bg-amber-100 text-amber-800", unavailable: "bg-rose-100 text-rose-800", disabled: "bg-slate-100 text-slate-700", misconfigured: "bg-orange-100 text-orange-800" };
export function IntegrationHero({ health }: { health: IntegrationHealth[] }) { const available = health.filter(item => item.state === "available").length; return <section className="overflow-hidden rounded-[28px] bg-[#0f5a46] text-white shadow-[0_24px_60px_rgba(15,90,70,.22)]"><div className="grid gap-8 p-7 lg:grid-cols-[1.15fr_.85fr] lg:p-10"><div><div className="mb-5 inline-flex items-center gap-2 rounded-full border border-white/20 bg-white/10 px-3 py-1.5 text-xs font-medium"><LightningBoltIcon/>Integration Fabric</div><h1 className="max-w-3xl text-3xl font-semibold tracking-[-.035em] sm:text-4xl">Reliable connections to energy, enterprise and Web3 infrastructure.</h1><p className="mt-4 max-w-2xl text-sm leading-6 text-emerald-50/85 sm:text-base">Each external provider is isolated behind a typed adapter. Live data is shown only when authenticity, freshness and schema validity are established.</p><div className="mt-7 flex flex-wrap gap-3"><a href="/docs/integrations" className="rounded-xl bg-white px-4 py-2.5 text-sm font-semibold text-[#0f5a46]">Read integration guide</a><a href="/settings/integrations" className="rounded-xl border border-white/25 px-4 py-2.5 text-sm font-semibold">Configure providers</a></div></div><div className="rounded-2xl border border-white/15 bg-black/10 p-5 backdrop-blur"><div className="flex items-center justify-between"><div><p className="text-xs uppercase tracking-[.18em] text-emerald-100/70">Runtime health</p><p className="mt-1 text-2xl font-semibold">{available}/{health.length} available</p></div><GlobeIcon className="h-7 w-7"/></div><div className="mt-5 space-y-2">{health.slice(0,6).map(item=><div key={item.provider} className="flex items-center justify-between rounded-xl bg-white/10 px-3 py-2.5"><span className="text-sm font-medium capitalize">{item.provider.replace("-"," ")}</span><span className={`inline-flex items-center gap-1.5 rounded-full px-2 py-1 text-[11px] font-semibold ${stateClass[item.state]}`}>{item.state === "available" ? <CheckCircledIcon/> : <ExclamationTriangleIcon/>}{item.state}</span></div>)}</div></div></div></section>; }
