# PowerChain AI Core

Framework-independent contracts for GridLLM configurations, user-owned provider connections, versioned agents, prompt templates, memory policies, LoRA adapters and usage records.

Stored credentials are referenced by encrypted credential identifiers and must never be returned to browser clients.
