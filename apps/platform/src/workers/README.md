# Platform Workers

Typed registry for background jobs used by telemetry ingestion, settlements,
notifications, firmware rollouts, and AI operations. Production deployments
should connect this interface to the selected durable queue provider.
