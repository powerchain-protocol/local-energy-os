# Machine-readable contracts

This directory contains contract manifests and machine-readable artifacts. Normative documentation is published under `docs/contracts/m`; executable registries live in `packages/contracts`, and on-chain logic lives under `programs`.
