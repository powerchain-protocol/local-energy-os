export type EmailTemplateProps = { title: string; preview: string; body: string; actionLabel?: string; actionUrl?: string };
const escapeHtml = (value: string) => value.replace(/[&<>"']/g, character => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#039;" }[character] ?? character));
export function emailTemplate({ title, preview, body, actionLabel, actionUrl }: EmailTemplateProps) {
  const safeUrl = actionUrl && /^https:\/\//.test(actionUrl) ? actionUrl : undefined;
  const action = actionLabel && safeUrl ? `<p style="margin:28px 0"><a href="${escapeHtml(safeUrl)}" style="background:#0F5A46;color:#fff;padding:12px 18px;border-radius:10px;text-decoration:none;font-weight:700">${escapeHtml(actionLabel)}</a></p>` : "";
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"></head><body style="margin:0;background:#f8fafc;font-family:Inter,Arial,sans-serif;color:#0f172a"><div style="display:none;max-height:0;overflow:hidden">${escapeHtml(preview)}</div><main style="max-width:600px;margin:40px auto;background:#fff;border:1px solid #e2e8f0;border-radius:18px;padding:32px"><div style="font-weight:800;font-size:22px;color:#0F5A46">PowerChain</div><div style="font-size:12px;color:#64748b;margin-top:3px">Renewable Energy Operating System</div><h1 style="font-size:28px;line-height:1.2">${escapeHtml(title)}</h1><p style="font-size:16px;line-height:1.7;color:#475569">${escapeHtml(body)}</p>${action}<hr style="border:0;border-top:1px solid #e2e8f0;margin:30px 0"><p style="font-size:12px;color:#64748b">This operational message was sent by PowerChain. Review notification preferences in your profile.</p></main></body></html>`;
}
