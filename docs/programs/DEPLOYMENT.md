# Program deployment

Deploy to a local validator and devnet before any controlled mainnet release. Record the program hash, program ID, upgrade authority, cluster, build toolchain, and release commit in the deployment change record.

Run the program test suite and platform smoke tests before promotion. Mainnet deployment requires multisignature approval, funded fee accounts, verified RPC endpoints, monitoring, and a documented rollback or pause procedure.
