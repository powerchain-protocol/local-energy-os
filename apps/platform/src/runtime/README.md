# PowerChain Runtime

This directory contains the canonical runtime layer for the PowerChain Renewable Energy Intelligence Cloud. Domain implementations remain independently testable and expose stable contracts through local index modules.
