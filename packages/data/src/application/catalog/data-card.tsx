import type{ReactNode}from"react";import{cn}from"@/utils/util";
export function DataCard({label,value,change,icon,footer,className}:{label:string;value:string;change?:string;icon?:ReactNode;footer?:ReactNode;className?:string}){const positive=!change?.startsWith("-");return <article className={cn("panel p-5",className)}><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[.14em] muted">{label}</p><strong className="mt-2 block text-2xl font-black tracking-tight">{value}</strong>{change&&<span className={cn("mt-1 block text-xs font-bold",positive?"text-emerald-700":"text-red-600")}>{change}</span>}</div>{icon&&<div className="grid h-10 w-10 place-items-center rounded-xl bg-emerald-950/5 text-emerald-700 dark:bg-emerald-300/10">{icon}</div>}</div>{footer&&<div className="mt-4 border-t border-[var(--border)] pt-3 text-xs muted">{footer}</div>}</article>}
