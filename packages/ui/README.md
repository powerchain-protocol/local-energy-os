# @powerchain/ui

Reusable PowerChain presentation primitives. The package follows shadcn-style composition, keeps focus and accessibility behavior explicit, and applies the PowerChain emerald/cyan energy palette without coupling primitives to application routes.

`ToastProvider` belongs near the application provider root. Call `useToast()` from client components to report success, error, warning, or informational outcomes.
