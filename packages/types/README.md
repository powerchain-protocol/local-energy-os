# @powerchain/types

The canonical owner for PowerChain TypeScript contracts and validation schemas. Runtime Zod schemas are under `src/schemas`; domain types are under `src/types`; machine-readable JSON schemas are under `src/schemas/json`.

Do not add competing `schema`, `schemas`, or migration trees elsewhere in the repository.
