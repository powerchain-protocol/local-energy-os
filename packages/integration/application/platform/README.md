# PowerChain Integration Surface

Compatibility export layer for blockchain, oracle, mapping, SaaS, and energy-system adapters. New implementation code should live under `src/lib/integrations/`; this folder exists for stable public imports and integration documentation.
