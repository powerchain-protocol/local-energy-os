/**
 * Migration orchestration facade.
 * Prisma's canonical migration files remain under prisma/migrations.
 */
export const MIGRATION_DIRECTORY = "prisma/migrations" as const;
export const MIGRATION_PROVIDER = "prisma" as const;
