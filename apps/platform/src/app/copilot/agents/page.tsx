import Link from "next/link";
import { COPILOT_AGENTS } from "@powerchain/copilot";
import { Shell } from "@/components/shell";
export default function CopilotAgentsPage(){return <Shell><div className="content-container space-y-6"><header className="copilot-product-hero"><span className="eyebrow">COPILOT WORKFORCE</span><h1>Renewable RWA specialist agents.</h1><p>Agents are scoped roles coordinated by the RWA Orchestrator. They reuse skills and inherit only the context required for the task.</p><div className="copilot-product-actions"><Link className="secondary" href="/copilot/agents/builder">Open Agent Builder</Link></div></header><section className="copilot-registry-grid">{COPILOT_AGENTS.map(agent=><article className="copilot-registry-card" key={agent.id}><span>{agent.id}</span><h3>{agent.name}</h3><p>{agent.purpose}</p><div className="copilot-tag-row">{agent.skills.map(skill=><span key={skill}>{skill}</span>)}</div><div className="copilot-tag-row">{agent.permissions.map(permission=><span key={permission}>{permission}</span>)}</div></article>)}</section></div></Shell>}
