# PowerChain Configuration

Centralized, typed configuration for routing, networks, feature flags, platform status, responsive breakpoints, and server-only provider settings. Client-safe values belong in `networks.ts`; secrets and privileged endpoints belong in `server-networks.ts` or `src/env/server.ts`.
