import{NextResponse}from"next/server";import{randomUUID}from"node:crypto";export async function POST(req:Request){const b=await req.json().catch(()=>({}));const amount=Number(b.mintableWh);if(!Number.isFinite(amount)||amount<=0)return NextResponse.json({error:"mintableWh must be positive"},{status:400});return NextResponse.json({mintId:randomUUID(),status:"requires_signature",network:b.network??"solana-devnet",tokenSymbol:amount>=1_000_000?"MWh":"kWh",amountWh:amount,createdAt:new Date().toISOString()},{status:201})}
