# Workspaces

Domain workspaces own role-specific views and orchestration components. Shared
infrastructure belongs in `src/lib`, `src/components`, or `src/common` rather
than being duplicated inside a workspace.
