import{NextResponse}from"next/server";import{measurementStore,attestationStore,tokenStore}from"@/lib/proof-of-energy/store";export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;const measurement=measurementStore.get(id);const attestation=[...attestationStore.values()].find(x=>x.measurementId===id);const token=[...tokenStore.values()].find(x=>x.measurementId===id);if(!measurement)return NextResponse.json({error:"Proof not found"},{status:404});return NextResponse.json({data:{measurement,attestation,token}})}
