import type { IntegrationHealth } from "@powerchain/integration/core";
export function IntegrationStatusGrid({ items }: { items: IntegrationHealth[] }) { return <section className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{items.map(item=><article key={item.provider} className="rounded-2xl border bg-white p-5 shadow-sm dark:bg-slate-950"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-medium uppercase tracking-wider text-slate-500">Provider</p><h2 className="mt-1 text-lg font-semibold capitalize">{item.provider.replace("-"," ")}</h2></div><span className="rounded-full border px-2.5 py-1 text-xs font-medium capitalize">{item.state}</span></div><dl className="mt-5 grid grid-cols-2 gap-3 text-sm"><div><dt className="text-slate-500">Checked</dt><dd className="mt-1 font-medium">{new Date(item.checkedAt).toLocaleTimeString()}</dd></div><div><dt className="text-slate-500">Circuit</dt><dd className="mt-1 font-medium capitalize">{item.circuitState ?? "n/a"}</dd></div><div><dt className="text-slate-500">Latency</dt><dd className="mt-1 font-medium">{item.latencyMs ? `${item.latencyMs} ms` : "No live response"}</dd></div><div><dt className="text-slate-500">Error</dt><dd className="mt-1 font-medium">{item.errorCode ?? "None"}</dd></div></dl></article>)}</section>; }
