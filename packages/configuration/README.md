# @powerchain/configuration

Canonical configuration boundary for route catalogs, redirects, network targets, release metadata, feature flags, breakpoints, cluster definitions, and validated client/server environment access. Browser code must use `env/client`; privileged values remain in `env/server`.
