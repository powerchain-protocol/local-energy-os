# MeterRegistry Program

Registers meters, owners, calibration state and monotonic sequence counters.

Targets: localnet, devnet and mainnet-beta. Production deployment requires audited program IDs and controlled upgrade authorities.
