# PowerChain AI Gateway

Server-side routing boundary for hosted GridLLM inference, organization models and user-owned model APIs. The gateway owns authentication, endpoint validation, rate limits, streaming, audit records and provider attribution.
