import { digitalEnergyError, digitalEnergyResponse, enforceDigitalEnergyRateLimit, getDigitalEnergyContext, requireIdempotencyKey, reserveDigitalEnergyPosition } from "@/lib/digital-energy/server";
export async function POST(request: Request, { params }: { params: Promise<{ id: string }> }) { const context=await getDigitalEnergyContext(request); try { enforceDigitalEnergyRateLimit(request,context,true); const {id}=await params; const body=await request.json() as {reservationId:string;amountWh:string;purpose?:string}; const data=await reserveDigitalEnergyPosition(context,{positionId:id,reservationId:String(body.reservationId),amountWh:String(body.amountWh),purpose:String(body.purpose??"LOCAL_MARKET"),idempotencyKey:requireIdempotencyKey(request)}); return digitalEnergyResponse(data,context,{status:201}); } catch(error){ return digitalEnergyError(error,context); } }
