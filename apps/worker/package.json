{
  "name": "@powerchain/app-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@powerchain/system-management": "workspace:*",
    "@powerchain/config": "workspace:*",
    "@powerchain/database": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "tsx": "^4.20.0",
    "@types/node": "^24.10.0"
  }
}
