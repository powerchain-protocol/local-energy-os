import{createCipheriv,randomBytes}from"node:crypto";export function encryptEnergyPayload(payload:unknown,key:Buffer){const nonce=randomBytes(12);const cipher=createCipheriv("aes-256-gcm",key,nonce);const ciphertext=Buffer.concat([cipher.update(JSON.stringify(payload)),cipher.final()]);return{algorithm:"AES-256-GCM"as const,nonce:nonce.toString("base64"),ciphertext:ciphertext.toString("base64"),authenticationTag:cipher.getAuthTag().toString("base64")}}
