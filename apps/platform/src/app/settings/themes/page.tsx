"use client";
import {SettingsLayout} from "@/components/profile/settings-layout";
import {dashboardThemes,themeLabels} from "@/themes";
import {Button} from "@/components/ui/button";
import {useTheme} from "@/context/theme-context";
export default function Themes(){const {theme:active,setTheme}=useTheme();return <SettingsLayout title="Dashboard themes" subtitle="Choose an interface treatment for operational, trading and executive workspaces."><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{dashboardThemes.map(theme=><article key={theme} className={`theme-preview theme-${theme} rounded-2xl p-5`} aria-label={`${themeLabels[theme]} theme`}><div className="flex items-center justify-between"><strong>{themeLabels[theme]}</strong><span className="text-xs font-semibold">{active===theme?"Active":"Preview"}</span></div><div className="mt-5 grid grid-cols-[.35fr_1fr] gap-3"><div className="h-24 rounded-lg border border-current/15 opacity-50"/><div className="space-y-3"><div className="h-8 rounded-lg border border-current/15 opacity-60"/><div className="h-12 rounded-lg border border-current/15 opacity-40"/></div></div><Button onClick={()=>setTheme(theme)} variant={theme==="red"?"red":theme==="white"?"white":theme==="black"?"black":theme==="onyx"?"onyx":theme==="framed"?"framed":"dark-green"} className="mt-5 w-full">{active===theme?"Applied":"Apply theme"}</Button></article>)}</div></SettingsLayout>}
