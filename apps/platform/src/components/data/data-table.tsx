import type { ReactNode } from "react";
export interface DataColumn<T> { key: keyof T | string; label: string; render?: (row: T) => ReactNode; }
export function DataTable<T extends { id?: string }>({ rows, columns, empty = "No records found" }: { rows: T[]; columns: DataColumn<T>[]; empty?: string }) {
  return <div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-950"><div className="overflow-x-auto"><table className="w-full min-w-[680px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500 dark:bg-slate-900"><tr>{columns.map(c => <th key={String(c.key)} className="px-4 py-3 font-semibold">{c.label}</th>)}</tr></thead><tbody className="divide-y divide-slate-100 dark:divide-slate-800">{rows.length ? rows.map((row, i) => <tr key={row.id ?? i} className="hover:bg-slate-50/70 dark:hover:bg-slate-900/60">{columns.map(c => <td key={String(c.key)} className="px-4 py-3 text-slate-700 dark:text-slate-200">{c.render ? c.render(row) : String((row as Record<string, unknown>)[String(c.key)] ?? "—")}</td>)}</tr>) : <tr><td colSpan={columns.length} className="px-4 py-10 text-center text-slate-500">{empty}</td></tr>}</tbody></table></div></div>;
}
