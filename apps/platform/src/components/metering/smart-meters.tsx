"use client";
import { useIoT } from "@/hooks/use-iot";
export function SmartMeters(){const{readings,status}=useIoT();return <div className="space-y-4"><div className="flex items-center justify-between"><div><h2 className="text-2xl font-extrabold">Smart meters</h2><p className="text-sm muted">Live LoRaWAN and grid-meter telemetry with safe fallback data.</p></div><span className="rounded-full border border-[var(--border)] px-3 py-1 text-xs font-bold">{status}</span></div><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{readings.map(item=><article key={`${item.deviceId}-${item.metric}`} className="rounded-2xl border border-[var(--border)] bg-[var(--surface)] p-4"><p className="text-xs font-bold uppercase tracking-wider muted">{item.deviceId}</p><p className="mt-3 text-3xl font-black">{item.value}<span className="ml-1 text-sm font-medium muted">{item.unit}</span></p><p className="mt-2 text-sm">{item.metric}</p><p className="mt-3 text-xs muted">Quality: {item.quality}</p></article>)}</div></div>}
