# On-chain programs

The `programs` workspace contains Anchor-compatible Solana programs for PowerChain settlement and proof workflows. Program interfaces are versioned with the platform release and must preserve account ownership, signer, amount, and replay-protection invariants.

Use the security, deployment, and testing guides in this directory before modifying or publishing a program. Production program IDs must replace all repository placeholders.
