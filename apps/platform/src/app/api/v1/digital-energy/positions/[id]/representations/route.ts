import { digitalEnergyError, digitalEnergyResponse, enforceDigitalEnergyRateLimit, getDigitalEnergyContext, representDigitalEnergyPosition, requireIdempotencyKey } from "@/lib/digital-energy/server";
export async function POST(request: Request, { params }: { params: Promise<{ id: string }> }) { const context=await getDigitalEnergyContext(request); try { enforceDigitalEnergyRateLimit(request,context,true); const {id}=await params; const body=await request.json() as {representationId:string;network:"SOLANA"|"SUI";reference:string;amountWh:string}; const data=await representDigitalEnergyPosition(context,{positionId:id,representationId:String(body.representationId),network:body.network,reference:String(body.reference),amountWh:String(body.amountWh),idempotencyKey:requireIdempotencyKey(request)}); return digitalEnergyResponse(data,context,{status:201}); } catch(error){ return digitalEnergyError(error,context); } }
