import{createHash,randomUUID}from"node:crypto";import type{EnergyAttestation,EnergyMeasurement}from"@/types/proof-of-energy";import{measurementFingerprint,netExportWh}from"./measurement";
export function createAttestation(m:EnergyMeasurement,validators:string[],threshold=0.67):EnergyAttestation{const ratio=validators.length/Math.max(3,validators.length);const verified=validators.length>=3&&ratio>=threshold;return{attestationId:randomUUID(),measurementId:m.measurementId,validatorIds:validators,verifiedWh:verified?netExportWh(m):0,confidenceScore:Math.min(.99,.72+validators.length*.05),consensusThreshold:threshold,status:verified?"verified":"rejected",proofHash:createHash("sha256").update(measurementFingerprint(m)).digest("hex"),verifiedAt:verified?new Date().toISOString():undefined}}
