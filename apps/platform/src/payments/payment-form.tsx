"use client";
import { useState } from "react";
export function PaymentForm({ onCheckout }: { onCheckout: (input:{amount:number; currency:string})=>Promise<void> }) { const [amount,setAmount]=useState(99); const [pending,setPending]=useState(false); return <form className="space-y-4" onSubmit={async(e)=>{e.preventDefault();setPending(true);try{await onCheckout({amount,currency:"USD"});}finally{setPending(false);}}}><label className="block text-sm font-medium">Amount<input className="mt-1 w-full rounded-xl border bg-transparent px-3 py-2" type="number" min="1" value={amount} onChange={e=>setAmount(Number(e.target.value))}/></label><button type="submit" disabled={pending} className="w-full rounded-xl bg-emerald-800 px-4 py-3 font-semibold text-white disabled:opacity-60">{pending?"Processing…":`Pay $${amount}`}</button></form>; }
