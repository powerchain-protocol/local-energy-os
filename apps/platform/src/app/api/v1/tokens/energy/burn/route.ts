import{NextResponse}from"next/server";import{burnToken}from"@/lib/tokenization";import{tokenStore}from"@/lib/proof-of-energy/store";export async function POST(req:Request){const{tokenId}=await req.json()as{tokenId:string};const token=tokenStore.get(tokenId);if(!token)return NextResponse.json({error:"Token not found"},{status:404});try{const data=burnToken(token);tokenStore.set(tokenId,data);return NextResponse.json({data})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Burn failed"},{status:400})}}
