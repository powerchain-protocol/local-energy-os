import { COPILOT_PROMPTS } from "@powerchain/copilot";
import { Shell } from "@/components/shell";
export default function CopilotPromptsPage(){return <Shell><div className="content-container space-y-6"><header className="copilot-product-hero"><span className="eyebrow">COPILOT PROMPT LIBRARY</span><h1>Start with operator intent, not prompt engineering.</h1><p>Prompt templates are simple starting points. Context comes from the current PowerChain workspace and explicit @ references.</p></header><section className="copilot-registry-grid">{COPILOT_PROMPTS.map(prompt=><article className="copilot-registry-card" key={prompt.id}><span>{prompt.category} · {prompt.mode}</span><h3>{prompt.title}</h3><p>{prompt.prompt}</p><div className="copilot-tag-row">{prompt.contexts.map(context=><span key={context}>@{context}</span>)}</div></article>)}</section></div></Shell>}
