# PowerChain Web

Public product entry point on port `3100`. The responsive landing surface has
one primary action—open the Command Center—and links to the configured platform
and documentation applications.

Run with `pnpm --filter @powerchain/web dev`.
