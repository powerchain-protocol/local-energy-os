"use client";import{useState}from"react";import type{PwrcBridgeDirection}from"@/types/bridge/pwrc";
export function usePwrcBridge(){const[loading,setLoading]=useState(false);const[error,setError]=useState<string|null>(null);async function quote(direction:PwrcBridgeDirection,amount:string){setLoading(true);setError(null);try{const r=await fetch(`/api/v1/bridge/pwrc/quote?direction=${direction}&amount=${encodeURIComponent(amount)}`);const p=await r.json();if(!r.ok)throw new Error(p.error);return p.data} catch(e){setError(e instanceof Error?e.message:"Bridge quote failed");return null}finally{setLoading(false)}}return{quote,loading,error}}
