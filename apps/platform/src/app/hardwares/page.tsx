import { DeviceTable } from "@/components/hardware";
import { hardwareDevices } from "@/data/hardware";
export default function HardwarePage(){const online=hardwareDevices.filter(d=>d.status==="online").length;return <main className="space-y-6"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-emerald-700">Edge operations</p><h1 className="mt-1 text-3xl font-extrabold tracking-tight">Hardware fleet</h1><p className="mt-2 text-[var(--muted)]">Provision, monitor, and maintain energy-edge devices across every organization and site.</p></div><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[["Devices",hardwareDevices.length],["Online",online],["Needs attention",hardwareDevices.length-online],["Sites",new Set(hardwareDevices.map(d=>d.site)).size]].map(([label,value])=><div key={label} className="rounded-2xl border border-[var(--border)] bg-[var(--surface)] p-5"><p className="text-sm text-[var(--muted)]">{label}</p><p className="mt-2 text-3xl font-extrabold">{value}</p></div>)}</section><DeviceTable devices={hardwareDevices}/></main>}
