# Documentation workspace

The `/docs` App Router workspace is the user-facing documentation hub. It consolidates architecture, API, AI, token, operations and legal references without duplicating the authoritative Markdown and machine-readable assets stored at the repository root.
