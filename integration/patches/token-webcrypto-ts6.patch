--- a/packages/token/src/solana/pda.ts
+++ b/packages/token/src/solana/pda.ts
@@
-  const digest = await globalThis.crypto.subtle.digest("SHA-256", input);
+  // TypeScript 6 models Uint8Array as Uint8Array<ArrayBufferLike>, while
+  // WebCrypto requires an ArrayBuffer-backed BufferSource. Copy the bytes
+  // into an owned ArrayBuffer to preserve identical SHA-256 input bytes.
+  const digestInput = new ArrayBuffer(input.byteLength);
+  new Uint8Array(digestInput).set(input);
+  const digest = await globalThis.crypto.subtle.digest("SHA-256", digestInput);
