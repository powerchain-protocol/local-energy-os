import { COPILOT_SKILLS } from "@powerchain/copilot";
import { Shell } from "@/components/shell";
export default function CopilotSkillsPage(){return <Shell><div className="content-container space-y-6"><header className="copilot-product-hero"><span className="eyebrow">COPILOT SKILLS</span><h1>Reusable capabilities for every agent.</h1><p>Skills separate capabilities from agent identity, so analysis, verification, capital and workflow functions can be shared safely across the workforce.</p></header><section className="copilot-registry-grid">{COPILOT_SKILLS.map(skill=><article className="copilot-registry-card" key={skill.id}><span>{skill.category}</span><h3>{skill.name}</h3><p>{skill.description}</p><div className="copilot-tag-row">{skill.permissions.map(permission=><span key={permission}>{permission}</span>)}</div><p><strong>Output:</strong> {skill.output}</p></article>)}</section></div></Shell>}
