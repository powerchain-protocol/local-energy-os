import { NextResponse } from "next/server";import { listCertificates,validateCertificateIssuance,certificateProof } from "@/lib/certification";import { certificateIssueSchema } from "@/schemas/certification";import { createId } from "@/utils/helpers";import { errorPayload,toPowerChainError } from "@/utils/errors";export async function GET(){return NextResponse.json({certificates:listCertificates()})}export async function POST(request:Request){try{const input=certificateIssueSchema.parse(await request.json());validateCertificateIssuance(input);return NextResponse.json({certificate:{id:createId(input.kind.toLowerCase()),...input,status:"pending",issuanceProof:certificateProof(input)}},{status:202})}catch(e){const x=toPowerChainError(e);return NextResponse.json(errorPayload(x),{status:x.status})}}
