# PowerChain Mapper

Geospatial operations app for grid topology and energy infrastructure. It consumes canonical grid/infrastructure APIs while keeping map-provider concerns outside the grid domain.

- Dev port: `3011`
- Canonical version: `1.0.0`
